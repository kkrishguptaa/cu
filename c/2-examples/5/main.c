#include <math.h>
#include <stdio.h>

int main() {
  double a=4,b=2,c,d;

  c = sqrt(a);

  printf("Square root is %lf\n", c);

  d = pow(a,b);

  printf("a to the power of b is %lf\n", d);

  return 0;
}
